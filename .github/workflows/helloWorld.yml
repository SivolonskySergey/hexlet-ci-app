name: hello-World
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps: 
      - uses: make setup
      - uses: make install
      - name: run tests
        run: make test
      - name: run lint
        run: make lint